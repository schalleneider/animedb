{
    "delay" : 1000,
    "queryLengthLimit" : 40,
    "startDateOffsetLimit" : 1,
    "base" : "SELECT A.* FROM AniList A",
    "criteria" : "WHERE A.Id NOT IN (SELECT AniListId FROM AniList_MyAnimeList) ORDER BY A.Id",
    "limit" : "LIMIT 10"
}